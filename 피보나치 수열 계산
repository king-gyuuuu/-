#include <stdio.h>

int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

int main() {
    int n;

    printf("몇 번째 피보나치 수를 원하시나요? ");
    scanf("%d", &n);

    printf("피보나치 수열의 %d번째 수는 %d입니다.\n", n, fibonacci(n));

    return 0;
}
